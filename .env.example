# Copy this file to `.env` and fill in real values.
# Never commit the real `.env` file.

# JWT — generate with: node -e "console.log(require('crypto').randomBytes(60).toString('base64'))"
JWT_SECRET=replace_me_with_an_80_char_base64_secret
JWT_EXPIRY=1d

# Server
NODE_ENV=development
PORT=3000